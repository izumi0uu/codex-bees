import {
  handleMemoryGet,
  handleMemoryList,
  handleMemorySearch,
  handleMemoryStore,
  handlePlan,
  handlePlanProfile,
  handlePlanProfileRanking,
  handlePlanProfiles,
  handlePlanQueue,
  handlePlanSwarm,
  handlePlanSwarmQueue,
  handleSwarmArchive,
  handleSwarmArchiveGet,
  handleSwarmArchiveList,
  handleSwarmBlock,
  handleSwarmBlockers,
  handleSwarmBrief,
  handleSwarmBundle,
  handleSwarmCancel,
  handleSwarmCheck,
  handleSwarmCloseout,
  handleSwarmDispatch,
  handleSwarmDispatchBundle,
  handleSwarmDone,
  handleSwarmGet,
  handleSwarmInit,
  handleSwarmOverview,
  handleSwarmQueue,
  handleSwarmReopen,
  handleSwarmRestore,
  handleSwarmStart,
  handleSwarmSync,
  handleSwarmUpdate,
  printSwarms
} from "./plan-swarm-memory.js";
import {
  handleLeaderAssignmentDispatch,
  handleLeaderAssignmentDispatchBundle,
  handleLeaderAssignmentDispatchPack,
  handleLeaderAssignmentLaunchPlan,
  handleLeaderAssignments,
  handleLeaderAssignmentRanking,
  handleLeaderQueue,
  handleLeaderWorkspace,
  handleTaskAdd,
  handleTaskAnnotate,
  handleTaskApprove,
  handleTaskArchive,
  handleTaskArchiveGet,
  handleTaskArchiveList,
  handleTaskAssignmentPickup,
  handleTaskAssignmentPreview,
  handleTaskBlock,
  handleTaskBrief,
  handleTaskCheck,
  handleTaskClaim,
  handleTaskDone,
  handleTaskGet,
  handleTaskHistory,
  handleTaskInbox,
  handleTaskNext,
  handleTaskPickup,
  handleTaskPickupPreview,
  handleTaskReopen,
  handleTaskReject,
  handleTaskRelease,
  handleTaskReport,
  handleTaskReview,
  handleTaskRestore,
  handleTaskUpdate,
  handleVerifierBundle,
  handleWorkerCloseout,
  handleWorkerHandoff,
  handleWorkerSession,
  printTasks
} from "./task-handlers.js";

export const COORDINATION_CLI_COMMAND_DISPATCH_ENTRIES = [
  ["plan:profiles", async () => handlePlanProfiles()],
  ["plan:profile", async () => handlePlanProfile()],
  ["plan:profile-ranking", async () => handlePlanProfileRanking()],
  ["plan", async () => handlePlan()],
  ["plan:queue", async () => handlePlanQueue()],
  ["plan:swarm", async () => handlePlanSwarm()],
  ["plan:swarm:queue", async () => handlePlanSwarmQueue()],
  ["task:list", async () => printTasks()],
  ["task:add", async () => handleTaskAdd()],
  ["task:get", async () => handleTaskGet()],
  ["task:archive:list", async () => handleTaskArchiveList()],
  ["task:archive:get", async () => handleTaskArchiveGet()],
  ["task:history", async () => handleTaskHistory()],
  ["task:annotate", async () => handleTaskAnnotate()],
  ["task:report", async () => handleTaskReport()],
  ["task:brief", async () => handleTaskBrief()],
  ["task:inbox", async () => handleTaskInbox()],
  ["task:next", async () => handleTaskNext()],
  ["task:assignment-preview", async () => handleTaskAssignmentPreview()],
  ["task:assignment-pickup", async () => handleTaskAssignmentPickup()],
  ["task:pickup-preview", async () => handleTaskPickupPreview()],
  ["task:pickup", async () => handleTaskPickup()],
  ["worker:session", async () => handleWorkerSession()],
  ["worker:handoff", async () => handleWorkerHandoff()],
  ["worker:closeout", async () => handleWorkerCloseout()],
  ["verifier:bundle", async () => handleVerifierBundle()],
  ["leader:workspace", async () => handleLeaderWorkspace()],
  ["leader:queue", async () => handleLeaderQueue()],
  ["leader:assignments", async () => handleLeaderAssignments()],
  ["leader:assignment-ranking", async () => handleLeaderAssignmentRanking()],
  ["leader:assignment-dispatch", async () => handleLeaderAssignmentDispatch()],
  ["leader:assignment-dispatch-bundle", async () => handleLeaderAssignmentDispatchBundle()],
  ["leader:assignment-launch-plan", async () => handleLeaderAssignmentLaunchPlan()],
  ["leader:assignment-dispatch-pack", async () => handleLeaderAssignmentDispatchPack()],
  ["task:claim", async () => handleTaskClaim()],
  ["task:block", async () => handleTaskBlock()],
  ["task:review", async () => handleTaskReview()],
  ["task:approve", async () => handleTaskApprove()],
  ["task:reject", async () => handleTaskReject()],
  ["task:done", async () => handleTaskDone()],
  ["task:archive", async () => handleTaskArchive()],
  ["task:restore", async () => handleTaskRestore()],
  ["task:reopen", async () => handleTaskReopen()],
  ["task:release", async () => handleTaskRelease()],
  ["task:update", async () => handleTaskUpdate()],
  ["task:check", async () => handleTaskCheck()],
  ["swarm:init", async () => handleSwarmInit()],
  ["swarm:list", async () => printSwarms()],
  ["swarm:get", async () => handleSwarmGet()],
  ["swarm:archive:list", async () => handleSwarmArchiveList()],
  ["swarm:archive:get", async () => handleSwarmArchiveGet()],
  ["swarm:brief", async () => handleSwarmBrief()],
  ["swarm:bundle", async () => handleSwarmBundle()],
  ["swarm:blockers", async () => handleSwarmBlockers()],
  ["swarm:closeout", async () => handleSwarmCloseout()],
  ["swarm:archive", async () => handleSwarmArchive()],
  ["swarm:restore", async () => handleSwarmRestore()],
  ["swarm:reopen", async () => handleSwarmReopen()],
  ["swarm:dispatch-bundle", async () => handleSwarmDispatchBundle()],
  ["swarm:update", async () => handleSwarmUpdate()],
  ["swarm:check", async () => handleSwarmCheck()],
  ["swarm:overview", async () => handleSwarmOverview()],
  ["swarm:dispatch", async () => handleSwarmDispatch()],
  ["swarm:sync", async () => handleSwarmSync()],
  ["swarm:start", async () => handleSwarmStart()],
  ["swarm:block", async () => handleSwarmBlock()],
  ["swarm:done", async () => handleSwarmDone()],
  ["swarm:cancel", async () => handleSwarmCancel()],
  ["swarm:queue", async () => handleSwarmQueue()],
  ["memory:store", async () => handleMemoryStore()],
  ["memory:get", async () => handleMemoryGet()],
  ["memory:list", async () => handleMemoryList()],
  ["memory:search", async () => handleMemorySearch()]
];
